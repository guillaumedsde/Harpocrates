import React from "react";

//import { RestApiForPredictiveAnaltyics } from "js-api-client";

export default function DocumentSet({ name }) {
  return (
    <div>
      <h1>{name}</h1>
    </div>
  );
}
